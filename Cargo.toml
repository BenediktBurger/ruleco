[workspace]
members = [
    ".",
    "ruleco-core",
]

[package]
name = "ruleco"
version = "0.1.0"
authors = ["BB"]
edition = "2021"

[dependencies]
ruleco-core = { path = "ruleco-core", version = "0.1.0" }
zmq = "0.10.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
